# 2026-07-08 Local Full Suite

## Run Metadata

- Environment: `local`
- Repo: `/Users/preston/Code/zombie_invasion`
- Branch: `codex/dev-consolidation-20260707`
- Commit: `ba8978dcb923bd220e628f649e4b3e34dc73c389`
- Log directory: `/tmp/zombie-full-suite-local-20260708T154514Z`
- Local browser target: PlayCanvas route via Vite (`127.0.0.1`)
- Authenticated coverage: not applicable; this local game has no login surface.
- Dirty worktree: yes; soundtrack, smoke, CSS, tests, and docs changes were
  already present during validation.

## Suite Inventory

| Check | Status | Evidence |
|---|---|---|
| Finish-line focused Vitest: `npx vitest run test/music_assets.test.js test/music_director.test.js` | PASS | 2 files, 8 tests passed; log `01-finish-line-focused-vitest.log` |
| Full deterministic suite: `npm test` | PASS | 38 files, 204 tests passed; log `02-npm-test.log` |
| Production build: `npm run build` | PASS | Vite build completed with existing large-chunk warning; log `03-npm-build.log` |
| PlayCanvas browser smoke: `npm run smoke:playcanvas` | PASS | Local Vite smoke passed; screenshot `output/playcanvas-slice-smoke.png`; log `04-smoke-playcanvas.log` |
| Canonical full gate: `npm run test:full` | PASS | Re-ran `npm test`, `npm run build`, and `npm run smoke:playcanvas`; log `05-npm-test-full-canonical.log` |
| Supplemental web-game client | PASS with harness warning | Strict local server on `127.0.0.1:5177`; generated state/screenshot artifacts in `output/full-suite-web-game-local-20260708T154514Z-strict`; no `errors-*.json`; state reached `phase=running`, `musicCue=raid_mid` by iteration 2. The generic client still logged a selector timeout before actions, so treat this as supplemental coverage rather than the canonical browser gate. |

## Finish-Line Ledger Verification

Recent finish-line coverage from the generated soundtrack and toast-readability
work was included:

| Ledger item | Exact assertion/check | Result |
|---|---|---|
| Runtime soundtrack assets exist and are real MP3 files | `test/music_assets.test.js` via focused Vitest and `npm test` | PASS |
| Requested pressure-band remap is preserved | `raid_high.mp3` equals `boss_battle.mp3`; low/mid/high are otherwise distinct where expected | PASS |
| Motif and alternate shop renders stay reference-only | `main_motif.mp3` and `shop_intermission_alt.mp3` excluded from `MUSIC_CUES` | PASS |
| Adaptive music director cue mapping remains valid | `test/music_director.test.js` via focused Vitest and `npm test` | PASS |
| Top status toast readability is enforced in browser smoke | `npm run smoke:playcanvas` checks pointer-lock fallback copy is not nowrap/hidden/ellipsized and does not overflow | PASS |

## Browser Evidence

- `output/playcanvas-slice-smoke.png` regenerated by the canonical smoke.
- Supplemental client artifacts:
  - `output/full-suite-web-game-local-20260708T154514Z-strict/shot-0.png`
  - `output/full-suite-web-game-local-20260708T154514Z-strict/shot-1.png`
  - `output/full-suite-web-game-local-20260708T154514Z-strict/state-0.json`
  - `output/full-suite-web-game-local-20260708T154514Z-strict/state-1.json`

## Result

Local full suite is green. No code or asset fixes were required during the run.

Residual notes:

- Vite still reports the known large-chunk warning during build.
- Node still prints experimental localStorage warnings during Vitest.
- The generic web-game client's click-selector path is stale/noisy for this UI;
  the repo's canonical PlayCanvas smoke remains the authoritative browser gate.
